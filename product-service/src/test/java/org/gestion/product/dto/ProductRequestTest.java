package org.gestion.product.dto;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

import java.math.BigDecimal;

import static org.junit.jupiter.api.Assertions.assertEquals;

@SpringBootTest
public class ProductRequestTest {
/*
    @Test
    public void getter_setterProductRequestNameTest() {
        ProductRequest productRequest = new ProductRequest("baguette", "pas trop cuite", "01.30");
        assertEquals("baguette", productRequest.name());
        assertEquals("pas trop cuite", productRequest.description());
        assertEquals("1.30", productRequest.price());
    }
*/

}
