package org.gestion.product.dto;

import org.gestion.product.model.Product;
import org.junit.jupiter.api.Test;

import java.math.BigDecimal;

import static org.junit.jupiter.api.Assertions.assertEquals;

class ProductResponseTest {

    /*
    @Test
    void should_create_record() {

        Product product = new Product(
                "baguette",
                "MyDescription",
                "99.99"
        );

        assertEquals("baguette", product.name());
        assertEquals("MyDescription", product.description());
        assertEquals(new BigDecimal("99.99"), product.price());
    }

     */
}